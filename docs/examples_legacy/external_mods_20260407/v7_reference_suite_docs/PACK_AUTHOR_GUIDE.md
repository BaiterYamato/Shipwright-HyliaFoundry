# Pack Author Guide (v7)

1. Keep packs assets-only (no scripts/mod.json).
2. Use `materials/materials.json` with namespaced ids (`pack:*`).
3. Keep POM conservative and test LOD/fallback.
4. Validate with render inspector overlays.
