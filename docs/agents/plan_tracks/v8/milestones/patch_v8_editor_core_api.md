# Patch v8 Editor Core API milestone

- Parser/runtime gates for v8 editor capabilities.
- Direct hotkeys (`hotkeys[]`) and `actors.toggleArchetype` integration.
- External Mods UI segmentation and deterministic classification.
