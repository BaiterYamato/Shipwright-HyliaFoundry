# v8 Track — Editor + Builder

This track stores long-form planning for Patch v8 while executable entries remain append-only in `docs/agents/Plans.md`.

## Scope
- External Mods UI 3 tabs (`Resourcepacks`, `Mods`, `Core + API`)
- Direct hotkeys runtime (`hotkeys[]`) and `actors.toggleArchetype`
- Builder/editor capability scaffolding and reference suite sync

## Canonical Execution Ledger
- `docs/agents/Plans.md` (`PLN-20260303-0008`)
