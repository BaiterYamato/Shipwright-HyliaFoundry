# Patch v9 Core — Persistence / Narrative / Dev

## Goal
Deliver v9 core contracts and runtime behavior for persistence, narrative, and dev iteration.

## Core checkpoints
1. Capabilities + manifest fields + parser/validation.
2. Persistence runtime (domains/entities/migrations hooks).
3. Narrative runtime (flags/dialogue/quests/timeline state).
4. Hot reload/dev console/wasm sandbox v2 guardrails.
5. Actions/events registries + docs exports updated.
