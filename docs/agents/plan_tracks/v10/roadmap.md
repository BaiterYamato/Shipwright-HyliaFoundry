# v10 Roadmap

1. M1: packaging v2 schemas + lockfile + deterministic artifacts.
2. M2: capability tiers/permission contracts + grant storage model.
3. M3: `modtool validate` + `modtool pack` CI-grade reports.
4. M4: `modtool doctor` + conflict explainability.
5. M5: framework wiki + generated contract/registry indexes.
6. M6: starter templates and onboarding path.
7. M7: release-channel primitives with modloader UI gating for non-stable.
8. M8: hardening/failure quarantine and regression checks.
