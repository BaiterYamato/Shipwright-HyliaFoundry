# v10 Track — Ecosystem & Tooling

This track stores long-form planning for Patch v10 while executable progress remains append-only in `docs/agents/Plans.md`.

## Scope
- Packaging v2 (mods + packs) and lockfile workflow
- Capability/permission trust model scaffolding
- `modtool` CLI for init/validate/pack/doctor/diff/report
- Framework wiki + generated contract indexes + starter templates
- Release-channel primitives with UI gating (`stable` only until v1.0)

## Canonical Execution Ledger
- `docs/agents/Plans.md` (`PLN-20260304-0002`)
