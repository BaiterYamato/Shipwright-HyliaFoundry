# Smoke Content (Content Mod)

Generated by `modtool init`.

## Purpose
- Gameplay/content package that can depend on framework mods.

## Next steps
1. Add real capabilities and rationale strings.
2. Add dependencies to framework mods (semver ranges).
3. Run:
   - `tools/external_mods/modtool.ps1 validate -Path <this_mod>`
   - `tools/external_mods/modtool.ps1 doctor -Path <mods_root>`
   - `tools/external_mods/modtool.ps1 pack -Path <this_mod>`

